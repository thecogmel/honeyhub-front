import { LineAxis } from '@mui/icons-material';
import styled from 'styled-components';

export const ImageLogo = styled.img`
  width: 120px;
`;

export const SideBarButtonUIcon = styled(LineAxis)`
  width: 24px;
  height: 24px;
`;
